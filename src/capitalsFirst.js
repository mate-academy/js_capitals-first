'use strict';

/**
 * Implement capitalsFirst function:
 *
 * Create a function that takes an input String and returns a String, where all
 * the words of the input String that start with a capital letter are in front
 * and all the words that start with a lowercase letter or a non-alphabetic
 * character are at the end. The words that belong to the same category must be
 * left in the original order.
 *
 * If a word starts with a number or special character, skip the word and leave
 * it out of the result. Input String will not be empty.
 *
 * capitalFirst("hey You, Sort me Already!") === "You, Sort Already! hey me"
 *
 * @param {string} str
 *
 * @returns {string} - string with uppercase words in front
 */
function capitalsFirst(str) {
  // write code here
}

module.exports = capitalsFirst;
